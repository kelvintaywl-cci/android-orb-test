version: 2.1

jobs:
  job:
    parameters:
      message:
        type: string
    docker:
      - image: circleci/python:3
    steps:
      - run: echo hi

workflows:
  version: 2
  wf:
    triggers:
      - schedule:
          cron: "* * * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - job:
          message: hello
          
