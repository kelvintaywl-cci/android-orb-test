version: 2.1

jobs:
  job:
    parameters:
      message:
        type: string
    docker:
      - image: circleci/python:3
    steps:
      - run: echo << parameters.string >>

workflows:
  wf:
    jobs:
      - job:
          matrix:
            parameters:
              message: [hello, goodbye]
