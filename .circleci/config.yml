version: 2.1

jobs:
  job:
    parameters:
      matrix:
        type: boolean
    docker:
      - image: circleci/python:3
    steps:
      - run: echo hi

workflows:
  wf:
    jobs:
      - job:
          matrix: true
